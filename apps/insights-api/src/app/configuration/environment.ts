export enum Environment {
  Production = 'production',
  Development = 'development',
  Test = 'test',
}

export enum EnvironmentVariables {
  MYSQL_URI = 'MYSQL_URI',
  PORT = 'PORT',
  NODE_ENV = 'NODE_ENV',
}
